#!/usr/bin/env bash

declare -a preinstalled=(
    build-essential
    coreutils
    ethtool
    gnupg
    htop
    iproute2
    procps
    psmisc
    sysvinit-utils
    telnet
    unzip
    wget
    zenity
    zip
  );

declare -a postinstalled=(
    git
    jq
    openssh-server
    python-is-python3
  );

# declare -a awscloud=(
#     awscli
#     aws-iam-authenticator
#     saml2aws
#   );
#
# declare -a hashicorp=(
#     boundary
#     consul
#     nomad
#     packer
#     terraform
#     vagrant
#     vault
#     waypoint
#   );

## Disabling until SNAP is integrated.
## "atom:snap --classic"
## "pycharm-community:snap --classic"
## "postman:snap"
## "powershell:snap --classic"
## "code:snap"

## Requires user input.
## Note: It's not a suitable candidate for automatic deployment.
##       wireshark:apt

## Inpsecting these already installed devnetops tools
declare -a packages=(
    tree:apt
  );
